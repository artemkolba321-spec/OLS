#!/usr/bin/env bash
# OLS command: uedit
OLS_DIR="$HOME/OLS"
LOG_FILE="$OLS_DIR/logs.log"
FILE="$1"

EDITOR="${EDITOR:-nano}"

echo "Opening $FILE with $EDITOR..."
"$EDITOR" "$FILE"

echo "$(date '+%Y-%m-%d %H:%M:%S') [OLS][uedit][INFO] Edited $FILE using $EDITOR" >> "$LOG_FILE"
